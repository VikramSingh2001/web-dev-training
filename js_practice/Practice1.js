
    // constructor(s_no,name,price,discount,final_price,item_code){
    //     this.s_no = s_no;
    //     this.price = price;
    //     this.discount = discount;
    //     this.final_price = final_price;
    //     this.item_code = item_code;
    // }
import Practice2 from "./Practice2";
import React from 'react';
   function Practice1(){
    return (
        <div>
    {/* class ProductClass {
        constructor() {
            this.s_no = null;
            this.name = null;
            this.price = null;
            this.discount = null;
            this.final_price = null;
            this.item_code = null;
        }
    }
    import Practice1 from "./Practice2";
    let n = parseInt(prompt("Enter number of items")); // Convert input to integer
    let items = new Array(n); // Create an array of length n
    
    for (let i = 0; i < n; i++) {
        items[i] = new ProductClass();
    }
    
    function takeInput() {
        for (let i = 0; i < n; i++) {
            items[i].s_no = prompt(`Enter s_no for item ${i + 1}`);
            items[i].name = prompt(`Enter name for item ${i + 1}`);
            items[i].price = parseFloat(prompt(`Enter price for item ${i + 1}`));
            items[i].discount = parseFloat(prompt(`Enter discount in percentage for item ${i + 1}`));
            items[i].final_price = items[i].price - (items[i].price * items[i].discount / 100);
            console.log(`Item ${i + 1} final price is: ${items[i].final_price}`);
        }
    }
    
    function calculate() {
        takeInput();
    }
    
    calculate(); */}
    <app/>
    <Practice2/>
    </div>);
    }
    export default Practice1;
// for (let i = 0; i < 5; i++) {
// }
// for(var i =0;i<5;i++){

// }
// console.log(items);
// let items2 = [];
// function func(){
//     let obj = {}
//     for (let i = 0; i < 5; i++) {
//         items2[i] = new obj();
//     }
//     obj.S_No = prompt()
// }